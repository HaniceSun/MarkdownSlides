{
  "name": "marp-cli-example",
  "version": "0.0.0",
  "author": "Yuki Hattori <yukihattori1116@gmail.com>",
  "license": "WTFPL",
  "private": true,
  "scripts": {
    "build": "rimraf dist && npm run -s og-image && npm run -s deck",
    "deck": "marp --no-stdin projects/test/test.md -o dist/test/index.html && ncp assets dist/test/assets",
    "now-build": "npm run -s deck",
    "og-image": "marp PITCHME.md -o dist/test/og-image.jpg",
    "start": "marp -ps ."
  },
  "devDependencies": {
    "@marp-team/marp-cli": "^1.7.0",
    "ncp": "^2.0.0",
    "rimraf": "^3.0.2"
  }
}
